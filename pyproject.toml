[project]
name = "image-gen-service"
version = "0.1.0"
description = "HTTP service that launches tmux sessions and Codex CLI jobs"
requires-python = ">=3.10"

[project.scripts]
image-gen-service = "app.main:main"
